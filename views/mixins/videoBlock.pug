mixin videoBlock(video = {})
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video(src=video.fileUrl, type="video/webm", controls, width="250")
            h4=video.title
            h6=video.views

